'use strict';

var _asciify = require('./asciify');

var _asciify2 = _interopRequireDefault(_asciify);

var _addEaster = require('./addEaster');

var _addEaster2 = _interopRequireDefault(_addEaster);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

_addEaster2.default.push({
  name: 'ascii', // name
  code: [65, 83, 67, 73, 73],
  action: function action() {
    var imgs = document.querySelectorAll('img');
    var bgs = document.querySelectorAll('[style*="background"]');
    var vids = document.querySelectorAll('video');

    Array.from(imgs, _asciify2.default);
    Array.from(bgs, _asciify2.default);
    Array.from(vids, _asciify2.default);
  }
});
// import clippy from 'clippy';
// import cssfilters from 'cssfilters';

